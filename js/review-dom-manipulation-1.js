var para = document.querySelector('p');
console.log(para);

var allParagraphs = document.querySelectorAll('p');
console.log(allParagraphs);

allParagraphs[0].textContent = 'I have successfully used .textContent to change what the lorem had from html.';